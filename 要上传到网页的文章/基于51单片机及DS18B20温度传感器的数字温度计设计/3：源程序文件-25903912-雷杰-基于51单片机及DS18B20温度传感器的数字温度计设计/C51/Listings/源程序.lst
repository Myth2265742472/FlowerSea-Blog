C51 COMPILER V9.59.0.0   ______                                                            12/11/2025 00:03:04 PAGE 1   


C51 COMPILER V9.59.0.0, COMPILATION OF MODULE ______
OBJECT MODULE PLACED IN .\Objects\源程序.obj
COMPILER INVOKED BY: C:\Keil_v5\C51\BIN\C51.EXE 源程序.c OPTIMIZE(8,SPEED) BROWSE DEBUG OBJECTEXTEND PRINT(.\Listings\源
                    -程序.lst) TABS(2) OBJECT(.\Objects\源程序.obj)

line level    source

   1          #include <reg52.h>
   2          #include <stdio.h>
   3          
   4          #define uchar unsigned char
   5          #define uint unsigned int
   6          #define dula P0   //段选信号的锁存器控制
   7          
   8          // 数码管段码表
   9          unsigned char code table[] = {
  10              0x3f,0x06,0x5b,0x4f,0x66,0x6d,0x7d,
  11              0x07,0x7f,0x6f,0x77,0x7c,0x39,0x5e,0x79,0x71
  12          };
  13          
  14          // 带小数点的段码表
  15          unsigned char code table1[] = {
  16              0xbf,0x86,0xdb,0xcf,0xe6,0xed,0xfd,
  17              0x87,0xff,0xef
  18          };
  19          
  20          // 数码管位选信号
  21          sbit wei1 = P2^4;  // 第1位
  22          sbit wei2 = P2^5;  // 第2位
  23          sbit wei3 = P2^6;  // 第3位
  24          sbit wei4 = P2^7;  // 第4位
  25          sbit DS = P2^2;    // DS18B20数据线
  26          
  27          // 按键定义
  28          sbit K2 = P3^4;    // 模式切换键
  29          sbit K3 = P3^5;    // 减键/显示下限键
  30          sbit K4 = P3^6;    // 加键/显示上限键
  31          sbit K5 = P3^7;    // 按键音开关键
  32          
  33          // 蜂鸣器
  34          sbit BEEP = P2^3;  // 蜂鸣器控制
  35          
  36          // 全局变量
  37          uint temp;         // 温度变量
  38          uint temp_high = 300;  // 温度上限，默认30.0°C
  39          uint temp_low = 0;     // 温度下限，默认0.0°C
  40          uchar mode = 0;        // 0:正常模式 1:设置上限 2:设置下限
  41          bit show_high_flag = 0; // 显示上限标志
  42          bit show_low_flag = 0;  // 显示下限标志
  43          uint show_counter = 0;  // 显示计数器
  44          bit key_sound_enabled = 1; // 按键音开关，默认开启
  45          
  46          // 延时函数
  47          void delay(uint count) {
  48   1          uint i;
  49   1          while(count) {
  50   2              i = 200;
  51   2              while(i > 0)
  52   2                  i--;
  53   2              count--;
  54   2          }
C51 COMPILER V9.59.0.0   ______                                                            12/11/2025 00:03:04 PAGE 2   

  55   1      }
  56          
  57          // 毫秒延时函数
  58          void delay_ms(uint ms) {
  59   1          uint i, j;
  60   1          for(i = 0; i < ms; i++)
  61   1              for(j = 0; j < 120; j++);
  62   1      }
  63          
  64          // 按键音函数
  65          void play_key_sound() {
  66   1          if(key_sound_enabled) {
  67   2              BEEP = 0;    // 打开蜂鸣器
  68   2              delay_ms(50); // 短暂发声
  69   2              BEEP = 1;    // 关闭蜂鸣器
  70   2          }
  71   1      }
  72          
  73          // DS18B20复位
  74          void dsreset(void) {
  75   1          uint i;
  76   1          DS = 0;                 
  77   1          i = 103;           // 将总线拉低480us~960us
  78   1          while(i > 0) i--;
  79   1          DS = 1;            // 然后拉高总线
  80   1          i = 4;             // 15us~60us等待
  81   1          while(i > 0) i--;
  82   1      }
  83          
  84          // 读取一个位
  85          bit tmpreadbit(void) {
  86   1          uint i;
  87   1          bit dat;
  88   1          DS = 0;
  89   1          i++;          // i++ for delay
  90   1          DS = 1;
  91   1          i++;
  92   1          i++;
  93   1          dat = DS;
  94   1          i = 8;
  95   1          while(i > 0) i--;
  96   1          return(dat);
  97   1      }
  98          
  99          // 读取一个字节
 100          uchar tmpread(void) {
 101   1          uchar i, j, dat;
 102   1          dat = 0;
 103   1          for(i = 1; i <= 8; i++) {
 104   2              j = tmpreadbit();
 105   2              dat = (j << 7) | (dat >> 1);   // 读出的数据最低位在最前面
 106   2          }
 107   1          return(dat);
 108   1      }
 109          
 110          // 写一个字节到DS18B20
 111          void tmpwritebyte(uchar dat) {
 112   1          uint i;
 113   1          uchar j;
 114   1          bit testb;
 115   1          for(j = 1; j <= 8; j++) {
 116   2              testb = dat & 0x01;
C51 COMPILER V9.59.0.0   ______                                                            12/11/2025 00:03:04 PAGE 3   

 117   2              dat = dat >> 1;
 118   2              if(testb) {    // write 1
 119   3                  DS = 0;
 120   3                  i++;
 121   3                  i++;
 122   3                  DS = 1;
 123   3                  i = 8;
 124   3                  while(i > 0) i--;
 125   3              } else {
 126   3                  DS = 0;       // write 0
 127   3                  i = 8;
 128   3                  while(i > 0) i--;
 129   3                  DS = 1;
 130   3                  i++;
 131   3                  i++;
 132   3              }
 133   2          }
 134   1      }
 135          
 136          // DS18B20开始温度转换
 137          void tmpchange(void) {
 138   1          dsreset();
 139   1          delay(1);
 140   1          tmpwritebyte(0xcc);  // address all drivers on bus
 141   1          tmpwritebyte(0x44);  // initiates a single temperature conversion
 142   1      }
 143          
 144          // 获取温度值
 145          uint tmp() {
 146   1          float tt;
 147   1          uchar a, b;
 148   1          dsreset();
 149   1          delay(1);
 150   1          tmpwritebyte(0xcc);
 151   1          tmpwritebyte(0xbe);
 152   1          a = tmpread();    // 低八位
 153   1          b = tmpread();    // 高八位
 154   1          temp = b;
 155   1          temp <<= 8;       // two byte compose a int variable
 156   1          temp = temp | a;
 157   1          tt = temp * 0.0625; // 算出来的是测到的温度
 158   1          temp = tt * 10 + 0.5; // 为了显示温度后的小数点后一位并作出四舍五入
 159   1          return temp;
 160   1      }
 161          
 162          // 检测按键
 163          void check_keys() {
 164   1          // K2键：模式切换
 165   1          if(K2 == 0) {
 166   2              delay_ms(10); // 消抖
 167   2              if(K2 == 0) {
 168   3                  uchar old_mode = mode;
 169   3                  mode = (mode + 1) % 3; // 循环切换模式
 170   3                  
 171   3                  // 从设置模式切换回正常模式时，清除显示标志
 172   3                  if((old_mode == 1 || old_mode == 2) && mode == 0) {
 173   4                      show_high_flag = 0;
 174   4                      show_low_flag = 0;
 175   4                      show_counter = 0;
 176   4                  }
 177   3                  
 178   3                  play_key_sound(); // 播放按键音
C51 COMPILER V9.59.0.0   ______                                                            12/11/2025 00:03:04 PAGE 4   

 179   3                  while(!K2); // 等待按键释放
 180   3              }
 181   2          }
 182   1          
 183   1          // K3键：减键/显示下限键
 184   1          if(K3 == 0) {
 185   2              delay_ms(10); // 消抖
 186   2              if(K3 == 0) {
 187   3                  if(mode == 1) { // 设置上限模式
 188   4                      if(temp_high > temp_low + 10) temp_high -= 10; // 减1°C
 189   4                  } else if(mode == 2) { // 设置下限模式
 190   4                      if(temp_low > 0) temp_low -= 10; // 减1°C，最低0°C
 191   4                  } else { // 正常模式，显示下限温度1秒
 192   4                      show_low_flag = 1;
 193   4                      show_counter = 100; // 显示约1秒
 194   4                  }
 195   3                  play_key_sound(); // 播放按键音
 196   3                  while(!K3); // 等待按键释放
 197   3              }
 198   2          }
 199   1          
 200   1          // K4键：加键/显示上限键
 201   1          if(K4 == 0) {
 202   2              delay_ms(10); // 消抖
 203   2              if(K4 == 0) {
 204   3                  if(mode == 1) { // 设置上限模式
 205   4                      if(temp_high < 500) temp_high += 10; // 加1°C，最高50.0°C
 206   4                  } else if(mode == 2) { // 设置下限模式
 207   4                      if(temp_low < temp_high - 10) temp_low += 10; // 加1°C
 208   4                  } else { // 正常模式，显示上限温度1秒
 209   4                      show_high_flag = 1;
 210   4                      show_counter = 100; // 显示约1秒
 211   4                  }
 212   3                  play_key_sound(); // 播放按键音
 213   3                  while(!K4); // 等待按键释放
 214   3              }
 215   2          }
 216   1          
 217   1          // K5键：按键音开关
 218   1          if(K5 == 0) {
 219   2              delay_ms(10); // 消抖
 220   2              if(K5 == 0) {
 221   3                  key_sound_enabled = !key_sound_enabled; // 切换按键音状态
 222   3                  // 无论当前状态如何，都播放一次声音作为反馈
 223   3                  BEEP = 0;
 224   3                  delay_ms(100);
 225   3                  BEEP = 1;
 226   3                  while(!K5); // 等待按键释放
 227   3              }
 228   2          }
 229   1      }
 230          
 231          // 显示温度函数
 232          void display(uint temp_val, uchar disp_mode) {
 233   1          uchar bai, shi;
 234   1          uint int_temp = temp_val / 10;  // 获取温度整数部分
 235   1          
 236   1          bai = int_temp / 10;  // 温度的十位
 237   1          shi = int_temp % 10;  // 温度的个位
 238   1      
 239   1          // 完全关闭所有数码管
 240   1          wei1 = 0;
C51 COMPILER V9.59.0.0   ______                                                            12/11/2025 00:03:04 PAGE 5   

 241   1          wei2 = 0;
 242   1          wei3 = 0;
 243   1          wei4 = 0;
 244   1          P0 = 0x00; // 关闭所有段
 245   1          delay(1);
 246   1      
 247   1          // 显示十位
 248   1          wei1 = 1;   
 249   1          wei2 = 0;
 250   1          wei3 = 0;
 251   1          wei4 = 0;
 252   1          
 253   1          if(disp_mode == 1) { // 设置上限模式
 254   2              P0 = table[bai] | 0x80; // 显示H符号（小数点）
 255   2          } else if(disp_mode == 2) { // 设置下限模式
 256   2              // 使用小数点位置闪烁来表示下限模式
 257   2              P0 = table[bai] | 0x80; // 使用小数点而不是g段来标识
 258   2          } else { // 正常温度显示
 259   2              P0 = table[bai];
 260   2          }
 261   1          delay(2);
 262   1      
 263   1          // 完全关闭所有数码管
 264   1          wei1 = 0;
 265   1          wei2 = 0;
 266   1          wei3 = 0;
 267   1          wei4 = 0;
 268   1          P0 = 0x00; // 关闭所有段
 269   1          delay(1);
 270   1      
 271   1          // 显示个位
 272   1          wei1 = 0;   
 273   1          wei2 = 1;
 274   1          wei3 = 0;
 275   1          wei4 = 0;
 276   1          P0 = table[shi];
 277   1          delay(2);
 278   1          
 279   1          // 完全关闭所有数码管
 280   1          wei1 = 0;
 281   1          wei2 = 0;
 282   1          wei3 = 0;
 283   1          wei4 = 0;
 284   1          P0 = 0x00; // 关闭所有段
 285   1          delay(1);
 286   1          
 287   1          // 显示空
 288   1          wei1 = 0;   
 289   1          wei2 = 0;
 290   1          wei3 = 1;
 291   1          wei4 = 0;
 292   1          P0 = 0x00; // 不显示任何内容
 293   1          delay(2);
 294   1          
 295   1          // 完全关闭所有数码管
 296   1          wei1 = 0;
 297   1          wei2 = 0;
 298   1          wei3 = 0;
 299   1          wei4 = 0;
 300   1          P0 = 0x00; // 关闭所有段
 301   1          delay(1);
 302   1          
C51 COMPILER V9.59.0.0   ______                                                            12/11/2025 00:03:04 PAGE 6   

 303   1          // 显示摄氏度符号
 304   1          wei1 = 0;   
 305   1          wei2 = 0;
 306   1          wei3 = 0;
 307   1          wei4 = 1;
 308   1          P0 = 0x39; // 显示C
 309   1          delay(2);
 310   1      }
 311          
 312          // 检查温度报警
 313          void check_alarm(uint current_temp) {
 314   1          if(current_temp > temp_high || current_temp < temp_low) {
 315   2              BEEP = 0; // 打开蜂鸣器
 316   2          } else {
 317   2              BEEP = 1; // 关闭蜂鸣器
 318   2          }
 319   1      }
 320          
 321          // 串口初始化函数
 322          void init_com(void) {
 323   1          TMOD = 0x20;       // 设T1为方式2
 324   1          SCON = 0x50;
 325   1          TH1 = 0xFD;
 326   1          TL1 = 0xFD;
 327   1          TR1 = 1;         // 开启定时器
 328   1          TI = 1;
 329   1          EA = 1;        // 开启总中断
 330   1      }
 331          
 332          // 主函数
 333          void main() {
 334   1          uchar a;
 335   1          uint temp_value = 0; // 温度
 336   1          uint display_temp = 0; // 显示的温度
 337   1          
 338   1          init_com();
 339   1          
 340   1          do {
 341   2              tmpchange();    // 让18b20开始转换温度
 342   2              temp_value = tmp();
 343   2              
 344   2              // 确保温度在0-50度范围内
 345   2              if(temp_value > 500) temp_value = 500; // 限制最高50.0°C
 346   2              
 347   2              // 检查按键
 348   2              check_keys();
 349   2              
 350   2              // 检查温度报警
 351   2              check_alarm(temp_value);
 352   2              
 353   2              // 决定显示什么温度
 354   2              if(show_high_flag && show_counter > 0) {
 355   3                  display_temp = temp_high; // 显示上限温度
 356   3                  show_counter--;
 357   3                  if(show_counter == 0) show_high_flag = 0;
 358   3              } else if(show_low_flag && show_counter > 0) {
 359   3                  display_temp = temp_low; // 显示下限温度
 360   3                  show_counter--;
 361   3                  if(show_counter == 0) show_low_flag = 0;
 362   3              } else if(mode == 1) {
 363   3                  display_temp = temp_high; // 设置上限模式，显示上限温度
 364   3              } else if(mode == 2) {
C51 COMPILER V9.59.0.0   ______                                                            12/11/2025 00:03:04 PAGE 7   

 365   3                  display_temp = temp_low;  // 设置下限模式，显示下限温度
 366   3              } else {
 367   3                  display_temp = temp_value; // 正常模式，显示当前温度
 368   3              }
 369   2              
 370   2              // 显示温度，循环显示一段时间保证稳定
 371   2              for(a = 50; a > 0; a--) {
 372   3                  display(display_temp, mode);
 373   3              }
 374   2              
 375   2              // 串口输出当前温度
 376   2              printf("$51,TMS%d#", temp_value / 10);
 377   2          } while(1);
 378   1      }


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =   1060    ----
   CONSTANT SIZE    =     37    ----
   XDATA SIZE       =   ----    ----
   PDATA SIZE       =   ----    ----
   DATA SIZE        =      9      12
   IDATA SIZE       =   ----    ----
   BIT SIZE         =      3       2
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
